; (function () {
    "use strict";

    var nsString = "Data.Archives.WebSQL.Operators", ns = HomeStock.Import(nsString);

    ns.Export("Reader", Reader);
    var messagePrefix = nsString + ".Reader: ";

    var Reader = function (params) {
        var self = this;

    };
})();