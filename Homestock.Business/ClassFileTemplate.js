; (function () {
    "use strict";

    var nsString = "", ns = HomeStock.Import(nsString);
    var messagePrefix = nsString + ".: ";

    ns.Export("", );

    function  (params) {
        this.validate(params, "");
        var self = this;

    };
})();